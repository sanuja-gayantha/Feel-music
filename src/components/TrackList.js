import React from 'react';

// custom imports

const TrackList = () => {

    // const { data, isLoading } = useGlobalContext()
    // if (isLoading) {
    //   return <Loader />
    // }
    // if (data.length < 1) {
    //   return (
    //     <h2 className='section-title'>
    //       no lyric matched y  our search criteria
    //     </h2>
    //   )
    // }
    return(
        <div className="track-list">
            <h2>Tracklist component</h2>
        </div>
    );
}

export default TrackList;